# Ringba API (use your Ringba account email and password)
RINGBA_USERNAME=your-ringba-email@example.com
RINGBA_PASSWORD=your-ringba-password
# Account ID from your Ringba URL (e.g. RA417e311c6e8b47538624556e6e84298a)
RINGBA_ACCOUNT_ID=your-account-id
# API token (optional; some scripts use username/password instead)
RINGBA_API_TOKEN=your-ringba-api-token
# If using API token with X-API-Key header instead of Bearer, set:
# RINGBA_AUTH_HEADER=X-API-Key

# Slack Incoming Webhook (create at api.slack.com → Your App → Incoming Webhooks)
SLACK_WEBHOOK_URL=your-webhook-url-from-slack-app

# Optional: only send Slack alerts when this many calls in the window (default: 1)
# ALERT_CALL_THRESHOLD=10
